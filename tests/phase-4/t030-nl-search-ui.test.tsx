import { describe, it, expect } from 'vitest'

describe('T030 — Natural Language Search UI', () => {
  it('should render a separate search mode for NL queries', () => {
    // TODO: render <NLSearch /> and verify distinct NL search input/toggle
    expect(true).toBe(false)
  })

  it('should display AI reasoning alongside results', () => {
    // TODO: mock NL search response with reasoning, verify reasoning text is rendered
    expect(true).toBe(false)
  })
})
