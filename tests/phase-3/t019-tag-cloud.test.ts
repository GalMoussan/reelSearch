import { describe, it, expect } from 'vitest'

describe('T019 — Tag Cloud API', () => {
  it('should return tags with reel count via GET /api/tags', () => {
    // TODO: call GET /api/tags and verify response shape [{ id, name, count }]
    expect(true).toBe(false)
  })

  it('should return tags sorted by count descending', () => {
    // TODO: verify tags[0].count >= tags[1].count >= tags[2].count
    expect(true).toBe(false)
  })
})
